% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef Directory < handle
  properties
    name
    entries
  end

  methods
    function self = Directory(kwargs)
      arguments
        kwargs.name = "";
        kwargs.entries = sketch.DirectoryEntry.empty();
      end
      self.name = kwargs.name;
      self.entries = kwargs.entries;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "sketch.Directory") && ...
        isequal(self.name, other.name) && ...
        isequal(self.entries, other.entries);
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = sketch.Directory();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
