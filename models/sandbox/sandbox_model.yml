# This is my enum
MyFlags: !flags
  values:
    # This is the "A" value
    - a
    - b
    - c

MyRec<T>: !record
  fields:
    f1: int?
    f2: T
    f3: MyFlags

# This is a string
MyString: string

R2<T>: !record
  fields:
    r2: MyRec<T>

R: MyRec<string>

P1: !protocol
  sequence:
    anInt: int
    aStream: !stream
      items: int
