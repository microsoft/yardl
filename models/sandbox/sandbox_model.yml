# This is my enum
MyFlags: !flags
  base: uint32
  values:
    # This is the "A" value
    - a
    - b
    - c

# MyRec<T>: !record
#   fields:
#     f1: int?
#     f2: T
#     f3: MyFlags

# # This is a string
# MyString: string

# R2<T>: !record
#   fields:
#     r2: MyRec<T>

# R: MyRec<string>

# MyInt: [int, float]

Point: !record
  fields:
    x: int
    y: int

P1: !protocol
  sequence:
    anInt: uint
    aStream: !stream
      items: int
    optional: int?
    union: [null, int, uint]
    date: date
    flag: MyFlags
    vec: int*3
    arr: uint[]
    map: string->int
